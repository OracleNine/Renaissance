import { WikiHomeUrlPipe } from './wiki-home-url-pipe';

describe('WikiHomeUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new WikiHomeUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
